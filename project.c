#define Project_UNDEFINED

const char* state = "beginning";


const char* why = "I just try to do something for my self";

const char* about_task =
  "I use neovim that always running in the root directory(the \"project.c\" exist place). \n"
  "if you want use those batch files, must call it on the root directory(for the stupid working directory). \n"
;


